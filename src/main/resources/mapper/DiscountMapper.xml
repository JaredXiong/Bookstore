<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookstore.mapper.DiscountMapper">
    <insert id="insert" parameterType="com.bookstore.entity.Discount">
        INSERT INTO `discount` (`discount_type`, `discount_rate`, `applicable_books`, `start_time`, `end_time`)
        VALUES (#{discountType}, #{discountRate}, #{applicableBooks}, #{startTime}, #{endTime})
    </insert>

    <select id="selectById" parameterType="java.lang.Integer" resultType="com.bookstore.entity.Discount">
        SELECT `discount_id`, `discount_type` as discountType, `discount_rate` as discountRate, 
               `applicable_books` as applicableBooks, `start_time` as startTime, `end_time` as endTime
        FROM `discount`
        WHERE `discount_id` = #{discountId}
    </select>

    <select id="selectAll" resultType="com.bookstore.entity.Discount">
        SELECT `discount_id`, `discount_type` as discountType, `discount_rate` as discountRate, 
               `applicable_books` as applicableBooks, `start_time` as startTime, `end_time` as endTime
        FROM `discount`
        ORDER BY `start_time` DESC
    </select>

    <select id="selectCurrent" resultType="com.bookstore.entity.Discount">
        SELECT `discount_id`, `discount_type` as discountType, `discount_rate` as discountRate, 
               `applicable_books` as applicableBooks, `start_time` as startTime, `end_time` as endTime
        FROM `discount`
        WHERE `start_time` &lt;= NOW()
          AND `end_time` &gt;= NOW()
        ORDER BY `start_time` ASC
    </select>

    <!-- 获取活跃折扣数量 -->
    <select id="getActiveDiscountCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM `discount` WHERE `start_time` &lt;= NOW() AND `end_time` &gt;= NOW()
    </select>

    <update id="update" parameterType="com.bookstore.entity.Discount">
        UPDATE `discount`
        SET `discount_type` = #{discountType},
            `discount_rate` = #{discountRate},
            `applicable_books` = #{applicableBooks},
            `start_time` = #{startTime},
            `end_time` = #{endTime}
        WHERE `discount_id` = #{discountId}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        DELETE FROM `discount`
        WHERE `discount_id` = #{discountId}
    </delete>
</mapper>