<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookstore.mapper.UserMapper">
    <insert id="insert" parameterType="com.bookstore.entity.User">
        INSERT INTO `user` (`username`, `password`, `email`, `address`, `postal_code`, `user_type`, `register_time`)
        VALUES (#{username}, #{password}, #{email}, #{address}, #{postalCode}, #{userType}, #{registerTime})
    </insert>
    
    <select id="selectById" resultType="com.bookstore.entity.User" parameterType="java.lang.Integer">
        SELECT `user_id` as userId, `username`, `password`, `email`, `address`, `postal_code` as postalCode, `user_type` as userType, `register_time` as registerTime
        FROM `user`
        WHERE `user_id` = #{userId}
    </select>
    
    <select id="selectByUsername" resultType="com.bookstore.entity.User" parameterType="java.lang.String">
        SELECT `user_id` as userId, `username`, `password`, `email`, `address`, `postal_code` as postalCode, `user_type` as userType, `register_time` as registerTime
        FROM `user`
        WHERE `username` = #{username}
    </select>
    
    <select id="selectAll" resultType="com.bookstore.entity.User">
        SELECT `user_id` as userId, `username`, `password`, `email`, `address`, `postal_code` as postalCode, `user_type` as userType, `register_time` as registerTime
        FROM `user`
    </select>
    
    <update id="update" parameterType="com.bookstore.entity.User">
        UPDATE `user`
        SET `username` = #{username}, `password` = #{password}, `email` = #{email}, `address` = #{address}, `postal_code` = #{postalCode}, `user_type` = #{userType}
        WHERE `user_id` = #{userId}
    </update>
    
    <delete id="delete" parameterType="java.lang.Integer">
        DELETE FROM `user`
        WHERE `user_id` = #{userId}
    </delete>
</mapper>