<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 引入外部属性文件 -->
    <properties resource="db.properties" />

    <!-- 配置环境 -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <!-- 使用 Druid 数据源 -->
            <dataSource type="com.bookstore.tool.DruidDataSourceFactory">
                <property name="driver" value="${jdbc.driver}" />
                <property name="url" value="${jdbc.url}" />
                <property name="username" value="${jdbc.username}" />
                <property name="password" value="${jdbc.password}" />
            </dataSource>
        </environment>
    </environments>

    <!-- 配置映射器 -->
    <mappers>
        <!-- 使用资源文件方式 -->
        <mapper resource="mapper/BookImageMapper.xml" />
        <mapper resource="mapper/BookMapper.xml" />
        <mapper resource="mapper/CommentMapper.xml" />
        <mapper resource="mapper/DiscountMapper.xml" />
        <mapper resource="mapper/OrderItemMapper.xml" />
        <mapper resource="mapper/OrderMapper.xml" />
        <mapper resource="mapper/ShoppingCartMapper.xml" />
        <mapper resource="mapper/UserMapper.xml" />
    </mappers>
</configuration>